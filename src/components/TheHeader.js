export default function TheHeader() {
  const firstName = "Samuele";

  return (
    <header>
      <h1 style={{ textAlign: "center", color: "red" }}>
        Ciao sono {firstName}
      </h1>
    </header>
  );
}
